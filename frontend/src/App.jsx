import React, { useState } from 'react'; import axios from 'axios'; import { Routes, Route, Navigate } from "react-router-dom"; const App = () => { const [isAuthenticated, setIsAuthenticated] = useState(false); const handleSignup = async (e) => { e.preventDefault(); const { username, password } = e.target.elements; await axios.post(import.meta.env.VITE_API_URL + '/api/auth/signup', { username: username.value, password: password.value }); Navigate('/login'); }; const handleLogin = async (e) => { e.preventDefault(); const { username, password } = e.target.elements; await axios.post(import.meta.env.VITE_API_URL + '/api/auth/login', { username: username.value, password: password.value }); setIsAuthenticated(true); }; return (<div><h1>Welcome to the App</h1><Routes><Route path="/signup" element={<form onSubmit={handleSignup}><input name="username" placeholder="Username" required /><input name="password" type="password" placeholder="Password" required /><button type="submit">Signup</button></form>} /><Route path="/login" element={<form onSubmit={handleLogin}><input name="username" placeholder="Username" required /><input name="password" type="password" placeholder="Password" required /><button type="submit">Login</button></form>} /><Route path="/dashboard" element={isAuthenticated ? <h2>Dashboard</h2> : <Navigate to="/login" />} /></Routes></div>); }; export default App;